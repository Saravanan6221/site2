pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                sh '''
                sudo rm -rf /var/www/site2/*
                sudo cp -r site2/* /var/www/site2/
                '''
            }
        }
    }
}
